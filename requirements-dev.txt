# Для локальной разработки: сервер + локальная расшифровка (без ключа) + парсинг + тесты.
-r requirements.txt
faster-whisper==1.2.1   # локальный Whisper (расшифровка без ключа OpenAI)
pymupdf==1.26.5         # парсинг исходных PDF (банк вопросов)
pytest==8.3.4
httpx==0.28.1
