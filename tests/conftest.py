"""Тесты всегда идут в мок-режиме: без обращения к OpenAI (быстро, бесплатно, детерминированно).

Боевой режим (gpt-4o / Whisper) проверяется отдельными ручными смоук-тестами.
"""
from backend.config import settings

# Сбрасываем ключ для всего набора тестов, даже если он задан в .env.
settings.openai_api_key = ""
